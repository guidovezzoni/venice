# 9.1.1 — Upgrade the Analytics Abstraction

**Epic:** Analytics
**Feature:** Analytics Foundation
**Status:** Refined

## User Story

As a developer, I want the analytics abstraction to be clearly named and safe by construction, so that
adding a new analytics backend is a one-line change and the debug sink cannot leak into a release
build.

## Description

Venice already has a provider-agnostic analytics layer (`AnalyticsTracker` / `AnalyticsProvider` /
`AnalyticsEvent`, `CompositeAnalyticsTracker`, `LogAnalyticsProvider`, wired via `AnalyticsModule`).
It works, but its naming diverges from the conventions in the analytics guidelines, and its Logcat
provider is active in **every** build type — including release, where Logcat is readable over ADB by
anyone with physical access to the device.

This story is a structural upgrade with **no change to which events are emitted or what they contain**.
That keeps it reviewable as a pure diff, separate from story 9.1.2 which changes the taxonomy itself.

The naming and documentation conventions follow the analytics guidelines at
`docs/guidelines/guidelines-analytics.md`.

### What changes

1. **Package relocation** — all analytics code moves from `domain/analytics/` and `data/analytics/` to
   `core/analytics/`, per the `core/` charter. Analytics carries no business rules; the domain layer
   should not know it is being observed.

2. **Renames** — interfaces, classes, and methods are renamed to align with the analytics guidelines
   and Firebase SDK naming (`logEvent`).

3. **Shared tracking surface** — a new `AnalyticsTracking` supertype declares `logEvent`,
   `setUserProperty`, and `trackException` once. Both `AnalyticsClient` and `AnalyticsProvider`
   extend it, preventing the current drift risk of redeclaring operations separately.

4. **User property API** — `setUserProperty(property: AnalyticsUserProperty)` is added to the
   abstraction. Nothing calls it in this story; the two concrete properties (`distance_unit`,
   `trip_count_band`) are defined and set by 9.1.2.

5. **Exception channel** — `trackException(throwable, operation)` is added as a separate channel from
   events. Crash reporting needs a stack trace, and the event taxonomy cannot carry one (its parameters
   are bounded enums by design). Nothing calls it in this story; 9.1.2 wires the call sites.

6. **Debug-only registration** — the Logcat provider's DI binding moves to the `debug` source set so
   it is never registered in release builds. The class stays in `main` so its unit test remains in the
   ordinary `src/test` source set.

7. **Log formatting extraction** — the Logcat message formatting is extracted into a dedicated internal
   function in its own file (`AnalyticsLogFormatter.kt`), making it directly unit-testable per the
   utility-function-in-own-file guideline.

8. **Minimal inline comments** — a warning comment on `DebugAnalyticsProvider` against release
   registration, and a comment on `CompositeAnalyticsClient` noting the zero-provider silent no-op.
   No KDoc beyond that.

9. **Test double for instrumented tests** — a `@TestInstallIn` module providing a recording fake
   replaces the real provider set in instrumented tests. None exists today.

## Acceptance Criteria

1. **Client renamed** — `AnalyticsClient` replaces `AnalyticsTracker`, exposing `logEvent(event)`.
2. **Provider renamed** — `AnalyticsProvider` exposes `logEvent(event)`.
3. **`shouldTrack` removed** — the per-provider event filter is dropped; providers that want to ignore
   certain events can no-op their own `logEvent` implementation.
4. **Implementations renamed** — `CompositeAnalyticsClient` and `DebugAnalyticsProvider`.
4. **All call sites updated** — both ViewModels use `analyticsClient.logEvent(...)`.
5. **Log formatting extracted** — the Logcat message is built by an `internal` function in its own
   file, not inline in the provider, so it is directly unit-testable.
6. **Minimal inline comments** — a warning comment on `DebugAnalyticsProvider` against release
   registration, and a comment on `CompositeAnalyticsClient` noting the zero-provider silent no-op.
   No full KDoc.
7. **Debug provider is bound in the debug source set only**, via a new
   `app/src/debug/java/.../di/DebugAnalyticsModule.kt`.
8. **Release variant compiles and runs with an empty provider set** — `AnalyticsModule` declares the
   multibound provider set explicitly so a variant with zero bindings still builds.
9. **A release build emits nothing** — no Logcat analytics output.
10. **A debug build behaves exactly as before** — same events, same parameters, same Logcat output
    apart from the changed log tag.
11. **Test double added** — a `@TestInstallIn` module replaces the provider set with a recording fake
    for instrumented tests. The fake records user properties and exceptions as well as events.
12. **No taxonomy change** — `AnalyticsEvent` and its sealed subclasses are untouched by this story.
13. **The tracking surface is declared once** in a shared `AnalyticsTracking` supertype;
    `AnalyticsClient` and `AnalyticsProvider` both extend it, and neither redeclares its operations.
14. **User property API exists** — `setUserProperty(property)` with `AnalyticsUserProperty` as a sealed
    class in `core/analytics`.
15. **Exception channel exists** — `trackException(throwable, operation)`, separate from events.
16. **`CompositeAnalyticsClient` fans events, user properties, and exceptions out to every provider**
    unconditionally.
17. **`DebugAnalyticsProvider` logs events, user properties, and exceptions distinguishably**, so
    on-device verification can tell the three apart in Logcat.
18. **A throwable never reaches an event parameter** — verified by inspection of the composite and the
    debug provider.
19. **The zero-provider case is a documented silent no-op** on `CompositeAnalyticsClient` — not an
    error, not a crash. This is the state a release build is in until 9.2.1 wires a backend.
20. **No call site uses the user property or exception APIs yet** — both are unused until 9.1.2, and
    that is intentional.
21. **All analytics code lives in `core/analytics/`** — `domain/analytics/` and `data/analytics/` no
    longer exist, and test files sit at the mirrored path.
22. **The move used `git mv`** so file history is preserved.
23. **No analytics type is referenced from `domain/` or `data/`** — verified by inspection; only
    `ui/viewmodel/` and `di/` may reference `core/analytics`.
24. **`core/` contains only `analytics/`** — nothing else was migrated as part of this story.

## Files To Be Created, Modified, Moved, or Deleted

### Files to move (via `git mv`)

These files move from their current location into the new `core/analytics/` package. The move is
combined with the rename.

| Current path | Target path | Notes |
|---|---|---|
| `domain/analytics/AnalyticsTracker.kt` | `core/analytics/AnalyticsClient.kt` | Rename interface and method |
| `domain/analytics/AnalyticsProvider.kt` | `core/analytics/AnalyticsProvider.kt` | Rename methods, remove `shouldTrack` |
| `domain/analytics/AnalyticsEvent.kt` | `core/analytics/AnalyticsEvent.kt` | Package declaration only |
| `data/analytics/CompositeAnalyticsTracker.kt` | `core/analytics/CompositeAnalyticsClient.kt` | Rename class and method, remove `shouldTrack` gate, add user property and exception fan-out |
| `data/analytics/LogAnalyticsProvider.kt` | `core/analytics/DebugAnalyticsProvider.kt` | Rename class, rename methods, extract log formatting |

All paths are relative to `app/src/main/java/com/guidovezzoni/venice/`.

### Files to create

| Path | Purpose |
|---|---|
| `core/analytics/AnalyticsTracking.kt` | Shared supertype declaring `logEvent`, `setUserProperty`, `trackException` |
| `core/analytics/AnalyticsUserProperty.kt` | Sealed class for user properties (initially empty body; properties defined by 9.1.2) |
| `core/analytics/AnalyticsOperation.kt` | Enum carrying the `operation` parameter values for `trackException` — already used as string constants in `AnalyticsEvent.Companion`; promoted to a proper type here |
| `core/analytics/AnalyticsLogFormatter.kt` | Extracted `internal` log-formatting function |
| `app/src/debug/java/com/guidovezzoni/venice/di/DebugAnalyticsModule.kt` | Hilt module with `@Binds @IntoSet` for `DebugAnalyticsProvider`, debug builds only |

### Files to modify

| Path | Change |
|---|---|
| `di/AnalyticsModule.kt` | Remove the `@Binds @IntoSet` for the debug provider; add `@Multibinds` declaration for the empty provider set; update import paths to `core.analytics` |
| `ui/viewmodel/TripListViewModel.kt` | Rename `analyticsTracker` to `analyticsClient`; change `track()` calls to `logEvent()`; update imports to `core.analytics` |
| `ui/viewmodel/TripDetailViewModel.kt` | Same rename and import update as above |

### Test files to move (via `git mv`)

| Current path | Target path | Notes |
|---|---|---|
| `app/src/test/.../domain/analytics/AnalyticsEventTest.kt` | `app/src/test/.../core/analytics/AnalyticsEventTest.kt` | Update package declaration |
| `app/src/test/.../data/analytics/CompositeAnalyticsTrackerTest.kt` | `app/src/test/.../core/analytics/CompositeAnalyticsClientTest.kt` | Rename test class, update references |
| `app/src/test/.../data/analytics/LogAnalyticsProviderTest.kt` | `app/src/test/.../core/analytics/DebugAnalyticsProviderTest.kt` | Rename test class, update references |

### Test files to create

| Path | Purpose |
|---|---|
| `app/src/test/.../core/analytics/AnalyticsLogFormatterTest.kt` | Unit tests for the extracted log-formatting function |
| `app/src/test/.../core/analytics/CompositeAnalyticsClientTest.kt` | New tests for user property fan-out and exception fan-out (extend existing test file after rename) |
| `app/src/androidTest/.../di/FakeAnalyticsModule.kt` | `@TestInstallIn` module replacing the provider set with a recording fake for instrumented tests |

### Test files to modify

| Path | Change |
|---|---|
| `app/src/test/.../ui/viewmodel/TripListViewModelTest.kt` | Rename `analyticsTracker` to `analyticsClient`; change `track()` mocking to `logEvent()`; update imports |
| `app/src/test/.../ui/viewmodel/TripDetailViewModelTest.kt` | Same rename and import update as above |

### Directories to delete

| Path | Reason |
|---|---|
| `domain/analytics/` | Empty after all files moved out |
| `data/analytics/` | Empty after all files moved out |

### Directories to create

| Path | Reason |
|---|---|
| `core/analytics/` | New package for all analytics code |
| `app/src/debug/java/com/guidovezzoni/venice/di/` | Source set for debug-only DI modules (currently only `app/src/debug/res/` exists) |

## DI Wiring Changes

### Current state

`AnalyticsModule` (in `main` source set) does two things:
1. Binds `CompositeAnalyticsTracker` as the `AnalyticsTracker` singleton
2. Binds `LogAnalyticsProvider` `@IntoSet` as an `AnalyticsProvider`

Both bindings exist in the `main` source set, meaning the debug provider is active in all build types.

### Target state

**`AnalyticsModule`** (in `main` source set) will:
1. Bind `CompositeAnalyticsClient` as the `AnalyticsClient` singleton
2. Declare an explicit `@Multibinds` for `Set<AnalyticsProvider>` so the release variant compiles
   with zero providers

**`DebugAnalyticsModule`** (new, in `debug` source set at `app/src/debug/java/.../di/`) will:
1. Bind `DebugAnalyticsProvider` `@IntoSet` as an `AnalyticsProvider` — debug builds only

**`FakeAnalyticsModule`** (new, in `androidTest` source set) will:
1. Use `@TestInstallIn` replacing `AnalyticsModule` (and `DebugAnalyticsModule`) to provide a
   recording fake that captures events, user properties, and exceptions for assertion

### Critical detail: `@Multibinds`

With the debug binding moved out of `main`, a release build has zero `@IntoSet` contributions. Dagger
will not synthesise an empty multibound set without an explicit `@Multibinds` declaration, so the
release variant fails to compile without it. This is the single most likely thing to be missed.

## Unit Tests

### Existing tests — rename and move

| Current | Target | What changes |
|---|---|---|
| `AnalyticsEventTest` | `AnalyticsEventTest` (same name, new package path under `core/analytics`) | Package declaration, imports |
| `CompositeAnalyticsTrackerTest` | `CompositeAnalyticsClientTest` (under `core/analytics`) | Class rename, all `track()` references become `logEvent()`, remove `shouldTrack()` tests, import paths |
| `LogAnalyticsProviderTest` | `DebugAnalyticsProviderTest` (under `core/analytics`) | Class rename, all `track()` references become `logEvent()`, remove `shouldTrack()` tests, import paths |

### New tests to add

| Test file | What it covers |
|---|---|
| `AnalyticsLogFormatterTest` | The extracted formatting function: events with and without properties, user properties, exceptions |
| Additional cases in `CompositeAnalyticsClientTest` | `setUserProperty` fans out to all providers; `trackException` fans out to all providers; zero-provider case for all methods is a silent no-op |
| Additional cases in `DebugAnalyticsProviderTest` | `setUserProperty` produces a distinguishable Logcat message; `trackException` produces a distinguishable Logcat message; the three message types (event, property, exception) are visually distinct |

### Existing ViewModel tests — update

Both `TripListViewModelTest` and `TripDetailViewModelTest` mock `AnalyticsTracker` and verify
`track()` calls. These must be updated to:
- Mock `AnalyticsClient` instead of `AnalyticsTracker`
- Rename the field from `analyticsTracker` to `analyticsClient`
- Change all `track()` verifications to `logEvent()`
- Update import paths from `domain.analytics` to `core.analytics`

No new ViewModel test cases are needed — the analytics behaviour itself is unchanged.

## Documentation Updates

| File | Change |
|---|---|
| `CLAUDE.md` (via `AGENTS.md` symlink) | Update the "Key Directories" table to include `core/` if not already present; update the source file listings under "Current Venice Analytics Files" if those are referenced |

No other documentation changes. The guidelines files (`guidelines-analytics.md`, `tracking-plan.md`)
already describe the target state — this story brings the code into alignment with them.

## Security

| Concern | Severity | Mitigation |
|---|---|---|
| **Debug provider in release builds** | High | This is the primary security fix of this story. Moving the DI binding to the `debug` source set ensures the Logcat provider is never registered in release. Verified by: `./gradlew assembleRelease` succeeds, and running the release build produces no Logcat analytics output. |
| **ADB-readable event stream** | Medium | Currently present in release builds. Eliminated by the debug-only binding. Between this story and 9.2.1, a release build has no provider at all — no events are emitted anywhere. |
| **Exception messages as event parameters** | Low | The current `OperationFailed` event already carries `error.message` as a parameter (a known PII vector per the tracking plan's migration table). This story does not fix that — it is 9.1.2's scope — but the new `trackException` channel provides the alternative path that makes the fix possible. The exception channel is explicitly documented as never routing through product analytics. |
| **Throwable in event parameters** | Low | The `trackException` API could be misused to route stack traces through the event taxonomy. Mitigated by: inline comment on `CompositeAnalyticsClient`; `AnalyticsProvider` explicitly ignoring exceptions in product-analytics implementations; code review gate. |

## Performance

| Concern | Severity | Mitigation |
|---|---|---|
| **Zero-provider overhead** | Negligible | With no providers bound in release (until 9.2.1), `logEvent` iterates an empty set. The cost is a no-op function call — undetectable. |
| **Thread safety** | Low | `CompositeAnalyticsClient` receives a `Set<AnalyticsProvider>` via constructor injection (immutable after construction). No `CopyOnWriteArrayList` or synchronization needed — Hilt guarantees the set is fully constructed before injection. The `DebugAnalyticsProvider` delegates to `android.util.Log.d`, which is thread-safe. |
| **Logcat volume in debug** | Negligible | The debug provider logs every event, user property, and exception. This is intentional for development. The volume is unchanged from current behaviour (events only) plus the new channels which are unused until 9.1.2. |

## GDPR and Sensitive Information

| Concern | Severity | Mitigation |
|---|---|---|
| **Current PII exposure via `error.message`** | Medium | `OperationFailed` currently passes `error.message` as a parameter. This is an unbounded cardinality, potential PII vector (Places/Directions API errors can contain user-entered place names). This story does not fix it — the taxonomy change to `error_type` is 9.1.2's scope — but it is called out here because awareness is needed. |
| **Current identifier exposure** | Medium | Events currently carry `tripId` and `stopId` as parameters. While these are local UUIDs (not tied to a user identity), they are still identifiers that should not reach analytics backends. This story does not fix it — 9.1.2 replaces them with aggregates — but the new abstraction is designed to support that fix. |
| **`trackException` carries throwables** | Low | Throwables can contain user-entered data in their message (e.g., a place name in a geocoding error). This is acceptable because the exception channel routes only to crash-reporting backends (Crashlytics), not product analytics. Crash reports are developer-facing tools, not analytics dashboards. |
| **User properties** | Low | The `AnalyticsUserProperty` API added here is empty — no properties are defined or set. Properties defined by 9.1.2 (`trip_count_band`, `distance_unit`) are bounded enums, not identifiers. The sealed-class design prevents ad-hoc properties from being added without code review. |
| **Consent posture** | Info | The app currently operates under an "assumed granted" consent posture (documented in the tracking plan). This story makes no change to that posture. Story 9.3.1 will add a consent gate. |

## Analytics

No new events are added. No existing events are changed. This story modifies only the delivery
mechanism — the abstraction layer through which events pass.

The one observable analytics behaviour change: **release builds stop emitting to Logcat**. Between
this story and 9.2.1, a release build has no provider at all and therefore emits nothing anywhere.
That is intended and temporary.

A user property API is introduced but left unused; the properties themselves are defined in the
tracking plan and set by 9.1.2.

An exception channel is introduced but left unused; 9.1.2 wires the call sites alongside the
`error_message` to `error_type` migration.

No new events are needed because this story is deliberately scoped to structural changes only. Adding
events in the same PR as renaming every analytics class would make the diff unreviewable — the
rename noise would hide the taxonomy changes. The separation into 9.1.1 (structure) and 9.1.2
(taxonomy) keeps each reviewable as a focused diff.

## Other Non-Functional Requirements

| Concern | Mitigation |
|---|---|
| **Build verification** | Both variants must be verified: `./gradlew assembleDebug assembleRelease`. A debug-only build passing proves nothing about the change that matters most (the removed release binding). |
| **Kover coverage** | The `di/` package is excluded from Kover (`*.di.*`), so the new `DebugAnalyticsModule` needs no test. The renamed analytics classes are **not** excluded and must keep clearing the 95% bound. All existing tests are renamed alongside their subjects. |
| **Detekt compliance** | All new and renamed files must pass `./gradlew detektDebug` with zero findings. |
| **File history preservation** | All moves use `git mv` so that `git log --follow` traces history through the rename. |
| **Backward compatibility** | No public API is exposed outside the app module, so renames have no downstream impact. The change is app-internal. |
| **Source set structure** | `app/src/debug/java/` does not currently exist — only `app/src/debug/res/`. The debug source set directory structure must be created as part of this story. |

## Technical Notes

- **`@Multibinds` is required, not optional.** With the debug binding moved out of `main`, a release
  build has zero `@IntoSet` contributions. Dagger will not synthesise an empty multibound set without
  an explicit declaration, so the release variant fails to compile. This is the single most likely
  thing to be missed.
- Verify **both** variants build: `./gradlew assembleDebug assembleRelease`.
- `di/` is excluded from Kover coverage (`*.di.*`), so the new debug module needs no test.
- Naming and documentation conventions: `docs/guidelines/guidelines-analytics.md`.

## Definition of Done

1. All acceptance criteria (1-24) are met
2. `./gradlew clean assembleDebug assembleRelease` succeeds with zero errors
3. `./gradlew test` passes with all unit tests green
4. `./gradlew detektDebug` passes with zero findings
5. `./gradlew koverVerify` passes the 95% coverage bound
6. A release build installed on a device produces no Logcat analytics output (verified via `adb logcat -s Analytics`)
7. A debug build installed on a device produces the same Logcat analytics output as before this change (apart from the changed log tag if applicable)
8. `domain/analytics/` and `data/analytics/` directories no longer exist
9. `core/analytics/` contains all analytics files and no other packages exist under `core/`
10. `git log --follow` traces each moved file's history through the rename
11. No `grep -r "AnalyticsTracker\|shouldTrack\|shouldLog\|\.track(" app/src/` matches remain (excluding git history)
12. No analytics types are imported from `domain.analytics` or `data.analytics` anywhere in the codebase

---

## Original user story

### User Story

As a developer, I want the analytics abstraction to be clearly named and safe by construction, so that
adding a new analytics backend is a one-line change and the debug sink cannot leak into a release
build.

### Description

Venice already has a provider-agnostic analytics layer (`AnalyticsTracker` / `AnalyticsProvider` /
`AnalyticsEvent`, `CompositeAnalyticsTracker`, `LogAnalyticsProvider`, wired via `AnalyticsModule`).
It works, but its naming diverges from the conventions in the analytics guidelines, and its Logcat
provider is active in **every** build type — including release, where Logcat is readable over ADB by
anyone with physical access to the device.

This story is a structural upgrade with **no change to which events are emitted or what they contain**.
That keeps it reviewable as a pure diff, separate from story 9.1.2 which changes the taxonomy itself.

The naming and documentation conventions follow the analytics guidelines at
`docs/guidelines/guidelines-analytics.md`.

#### Move to `core/`

Analytics currently straddles `domain/analytics/` and `data/analytics/`. Neither is right: it carries
no business rules, and no use case or repository references it — the domain layer should not know it is
being observed. The giveaway is `StopTypeParam`, which would sit in `domain/analytics/` purely to
serialise `domain/model/StopType` for a third-party SDK.

Everything analytics moves to a new **`core/analytics/`** package, parallel to `data/`, `domain/`, and
`ui/`, per the `core/` charter added to `guidelines-android.md`. Contracts and implementations stay
together, because analytics is one cohesive concern — the composite client is not a repository and the
providers map no DTOs.

```
core/analytics/    AnalyticsClient.kt            (from domain/analytics/AnalyticsTracker.kt)
                   AnalyticsProvider.kt          (from domain/analytics/)
                   AnalyticsEvent.kt             (from domain/analytics/)
                   AnalyticsUserProperty.kt      new, this story
                   CompositeAnalyticsClient.kt   (from data/analytics/CompositeAnalyticsTracker.kt)
                   DebugAnalyticsProvider.kt     (from data/analytics/LogAnalyticsProvider.kt)
                   AnalyticsLogFormatter.kt      new, this story
```

`domain/analytics/` and `data/analytics/` are removed entirely. Test files move to the mirrored
`core/analytics/` path under `src/test`.

**Do the move in this story, not later.** Every one of these files is already being renamed here, so
the move is nearly free; deferring it means churning the same files twice. Use `git mv` so the history
follows.

`core/` is created with `analytics/` as its **only** occupant. `ui/util/WithMinimumDuration.kt` and the
OkHttpClient provision in `di/NetworkModule.kt` are fair future candidates, but moving them is outside
this epic — later stories can move things in as they touch them.

#### Renames

| Current | Target |
|---------|--------|
| `AnalyticsTracker` | `AnalyticsClient` |
| `AnalyticsTracker.track(event)` | `AnalyticsClient.logEvent(event)` |
| `AnalyticsProvider.shouldTrack(event)` | Removed |
| `AnalyticsProvider.track(event)` | `AnalyticsProvider.logEvent(event)` |
| `CompositeAnalyticsTracker` | `CompositeAnalyticsClient` |
| `LogAnalyticsProvider` | `DebugAnalyticsProvider` |
| ViewModel field `analyticsTracker` | `analyticsClient` |

`logEvent` matches the Firebase SDK's own method name, which reduces friction when the Firebase
provider lands in 9.2.1. `DebugAnalyticsProvider` states *when the provider may be used*, which is the
constraint that matters about it.

#### User property API

The abstraction currently has no way to set a **user property** — a sticky attribute of the
installation that analytics backends attach to every subsequently logged event, used for segmenting
reports and defining audiences. Stories 9.1.2 and 9.2.1 both require user properties, so the API is
added here, where the abstraction lives.

```kotlin
interface AnalyticsTracking {
    fun logEvent(event: AnalyticsEvent)
    fun setUserProperty(property: AnalyticsUserProperty)
    fun trackException(throwable: Throwable, operation: AnalyticsOperation)
}

interface AnalyticsClient : AnalyticsTracking

interface AnalyticsProvider : AnalyticsTracking
```

The shared `AnalyticsTracking` supertype declares the surface **once**. Declaring `logEvent` separately
on the client and the provider lets the two drift, and every operation added then has to be remembered
in two places — a cost already paid twice while designing this epic.

`AnalyticsClient` adds nothing of its own. It exists to name what call sites depend on, keeping them
separate from `AnalyticsProvider`.

`AnalyticsUserProperty` is a sealed class in `core/analytics`, mirroring how `AnalyticsEvent` bounds
event parameters — the same reasoning applies, and more strongly: a user property is attached to
*every* event, so a high-cardinality one becomes a de facto persistent identifier.

**This story adds the API but nothing calls it.** The two concrete properties
(`distance_unit`, `trip_count_band`) are defined and set by 9.1.2.

#### Exception channel

`trackException(throwable, operation)` is a **separate channel from events**, added here for the same
reason as the user property API: 9.2.2 needs it and this is where the abstraction lives.

It exists because crash reporting needs a **stack trace**, and the event taxonomy cannot carry one.
`OperationFailed` is bounded to enum values by design — that is what keeps free text and PII out of
product analytics — so there is nowhere on it to put a throwable. Without a separate channel the
Crashlytics provider would record non-fatals with no stack trace, which is most of its value gone.

The throwable **must never** become an event parameter. Product-analytics providers ignore
`trackException`; only crash-reporting providers act on it.

As with user properties, this story adds the API and nothing calls it — 9.1.2 wires the call sites.

#### Debug-only registration

The Logcat provider must be registered in debug builds only. The gate is the **DI binding's source
set**, not a runtime flag:

```
app/src/main/java/.../core/analytics/DebugAnalyticsProvider.kt    class stays in main
app/src/debug/java/.../di/DebugAnalyticsModule.kt                 @Binds @IntoSet — debug only
```

The class stays in `main` so its unit test stays in the ordinary `src/test` source set and coverage
tooling needs no special case; R8 strips the unreferenced class from release builds.

Note that `app/src/debug/java/` does not exist yet — only `app/src/debug/res/`.

### Acceptance Criteria

1. **Client renamed** — `AnalyticsClient` replaces `AnalyticsTracker`, exposing `logEvent(event)`.
2. **Provider renamed** — `AnalyticsProvider` exposes `logEvent(event)`.
3. **`shouldTrack` removed** — the per-provider event filter is dropped; providers that want to ignore
   certain events can no-op their own `logEvent` implementation.
4. **Implementations renamed** — `CompositeAnalyticsClient` and `DebugAnalyticsProvider`.
4. **All call sites updated** — both ViewModels use `analyticsClient.logEvent(...)`.
5. **Log formatting extracted** — the Logcat message is built by an `internal` function in its own
   file, not inline in the provider, so it is directly unit-testable.
6. **Minimal inline comments** — a warning comment on `DebugAnalyticsProvider` against release
   registration, and a comment on `CompositeAnalyticsClient` noting the zero-provider silent no-op.
   No full KDoc.
7. **Debug provider is bound in the debug source set only**, via a new
   `app/src/debug/java/.../di/DebugAnalyticsModule.kt`.
8. **Release variant compiles and runs with an empty provider set** — `AnalyticsModule` declares the
   multibound provider set explicitly so a variant with zero bindings still builds.
9. **A release build emits nothing** — no Logcat analytics output.
10. **A debug build behaves exactly as before** — same events, same parameters, same Logcat output
    apart from the changed log tag.
11. **Test double added** — a `@TestInstallIn` module replaces the provider set with a recording fake
    for instrumented tests. None exists today, so instrumented tests currently hit the real provider.
    The fake records user properties as well as events.
12. **No taxonomy change** — `AnalyticsEvent` and its members are untouched by this story.
13. **The tracking surface is declared once** in a shared `AnalyticsTracking` supertype;
    `AnalyticsClient` and `AnalyticsProvider` both extend it, and neither redeclares its operations.
14. **User property API exists** — `setUserProperty(property)` with `AnalyticsUserProperty` as a sealed
    class in `core/analytics`.
15. **Exception channel exists** — `trackException(throwable, operation)`, separate from events.
16. **`CompositeAnalyticsClient` fans events, user properties, and exceptions out to every provider**
    unconditionally.
17. **`DebugAnalyticsProvider` logs events, user properties, and exceptions distinguishably**, so
    on-device verification can tell the three apart in Logcat.
18. **A throwable never reaches an event parameter** — verified by inspection of the composite and the
    debug provider.
19. **The zero-provider case is a documented silent no-op** on `CompositeAnalyticsClient` — not an
    error, not a crash. This is the state a release build is in until 9.2.1 wires a backend.
20. **No call site uses the user property or exception APIs yet** — both are unused until 9.1.2, and
    that is intentional.
21. **All analytics code lives in `core/analytics/`** — `domain/analytics/` and `data/analytics/` no
    longer exist, and test files sit at the mirrored path.
22. **The move used `git mv`** so file history is preserved.
23. **No analytics type is referenced from `domain/` or `data/`** — verified by inspection; only
    `ui/viewmodel/` and `di/` may reference `core/analytics`.
24. **`core/` contains only `analytics/`** — nothing else was migrated as part of this story.

### Analytics

No new events, no changed events. This story changes only the delivery mechanism.

The one observable analytics behaviour change: **release builds stop emitting to Logcat**. Between
this story and 9.2.1 a release build has no provider at all and therefore emits nothing anywhere. That
is intended and temporary.

A user property API is introduced but left unused; the properties themselves are defined in the
tracking plan and set by 9.1.2.

### Technical Notes

- **`@Multibinds` is required, not optional.** With the debug binding moved out of `main`, a release
  build has zero `@IntoSet` contributions. Dagger will not synthesise an empty multibound set without
  an explicit declaration, so the release variant fails to compile. This is the single most likely
  thing to be missed.
- Verify **both** variants build: `./gradlew assembleDebug assembleRelease`. A debug-only build passing
  proves nothing about the change that matters most here.
- `di/` is excluded from Kover coverage (`*.di.*`), so the new debug module needs no test. The renamed
  `data/analytics` and `domain/analytics` classes are **not** excluded and must keep clearing the 95%
  bound — the existing tests need renaming alongside their subjects.
- Naming and documentation conventions: `docs/guidelines/guidelines-analytics.md`.
