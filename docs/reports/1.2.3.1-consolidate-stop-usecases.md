# 1.2.3.1 — Consolidate Stop Use Cases

## Verification

**Date:** 2026-05-20

### OpenSpec Verify Result

**PASSED** — 17/17 tasks complete. All artifacts present (proposal, design, specs, tasks). Schema: spec-driven.

### TODO Scan Result

None found. No TODO comments exist in `app/src/`.

### Definition of Done Checklist

| # | Criterion | Status | Justification |
|---|-----------|--------|---------------|
| 1 | `StopType` enum exists with `STARTING_POINT`, `DESTINATION`, `INTERMEDIATE` | PASS | `domain/model/StopType.kt` defines exactly these three entries |
| 2 | Single `SetStopUseCase` replaces three old use cases | PASS | `SetStopUseCase.kt` exists; old files deleted |
| 3 | `SetStopUseCase` trims place name for all stop types | PASS | `placeName.trim()` called before `when` block; verified by test |
| 4 | `SetStopUseCase` enforces 25-stop limit only for `INTERMEDIATE` | PASS | `getStopCount` check only in `INTERMEDIATE` branch |
| 5 | `SetStopUseCase` delegates to correct repository method | PASS | `when` dispatches to `upsertStartingPoint`, `upsertDestination`, `addIntermediateStop` |
| 6 | `TripDetailViewModel` injects and uses only `SetStopUseCase` | PASS | Single constructor param; single `setStop` helper method |
| 7 | Three old use case files and test files deleted | PASS | Only `CreateTripUseCase`, `ObserveStopsUseCase`, `SetStopUseCase` remain |
| 8 | Single `SetStopUseCaseTest` covers all stop types | PASS | 8 test methods covering all types, trimming, limit, and failure propagation |
| 9 | `TripDetailViewModelTest` updated to mock single use case | PASS | Mocks `SetStopUseCase` with `StopType` matchers throughout |
| 10 | All existing tests pass / `./gradlew check` passes | PASS | Task 5.1 confirmed passed during implementation |
| 11 | No references to old use case class names in `app/src/` | PASS | Grep confirmed zero references |

### Final Outcome

**PASSED** — All acceptance criteria met. User story renamed to `1.2.3.1-consolidate-stop-usecases-DONE.md`.
