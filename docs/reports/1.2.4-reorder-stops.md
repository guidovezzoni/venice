# 1.2.4 — Reorder Stops

## Verification

**Date:** 2026-05-22

### OpenSpec Verify Result

All 8 task groups PASSED (tasks marked complete).

### TODO Scan Result

None found in `app/src/`.

### Definition of Done

| # | Criterion | Status | Justification |
|---|-----------|--------|---------------|
| 1 | Each intermediate stop shows move-up (unless first) and move-down (unless last) | PASS | `TripDetailScreen.kt` — conditional lambdas based on index position |
| 2 | Tapping move-up swaps with stop above; move-down swaps with below | PASS | Intent → ViewModel → MoveStopUseCase → StopRepository → StopDao.swapStopOrders |
| 3 | Starting point and destination cannot be moved and do not display reorder buttons | PASS | Only `items(intermediateStops)` loop receives move lambdas; start/destination sections have no move params |
| 4 | Reorder buttons not shown when fewer than 2 intermediate stops | PASS | `showReorderButtons = size >= MIN_REORDERABLE_STOPS` (constant = 2) |
| 5 | Stop list updates immediately after reordering via Flow-based observation | PASS | ViewModel observes `ObserveStopsUseCase` → `StopDao.observeByTripId` (Room Flow auto-emits on table change) |
| 6 | No data lost during reorder; only `order` field changes | PASS | `updateStopOrder` SQL: `UPDATE stops SET order = :newOrder WHERE id = :stopId` |

### Completion Checklist

| Item | Status |
|------|--------|
| `StopDao` has `getStopByTripIdAndOrder` and `updateStopOrder` methods | PASS |
| `StopRepository` interface has `swapStopOrder` method | PASS |
| `StopRepositoryImpl` implements `swapStopOrder` in a transaction | PASS — delegates to `StopDao.swapStopOrders` annotated with `@Transaction` |
| `MoveStopUseCase` validates and delegates the swap | PASS — delegates to repository; validation at DAO level (throws on missing stop) |
| `TripDetailUiIntent` has `OnMoveStopUp` and `OnMoveStopDown` | PASS |
| `TripDetailViewModel` handles the new intents | PASS |
| `StopSection` renders move-up/move-down buttons when lambdas are provided | PASS |
| `TripDetailScreen` passes reorder lambdas for intermediate stops | PASS |
| String resources added in EN, IT, ES | PASS |
| Unit tests for `MoveStopUseCase` pass (3 tests) | PASS |
| Unit tests for `TripDetailViewModel` reorder intents pass (3 tests) | PASS |
| Unit tests for `StopRepositoryImpl` swap pass (3 tests) | PASS |
| `./gradlew check` passes | PASS — BUILD SUCCESSFUL |
| Manual verification on emulator with 3+ intermediate stops | PASS — confirmed by user |

### Final Outcome

**PASSED**

Renamed: `1.2.4-reorder-stops-WIP.md` → `1.2.4-reorder-stops-DONE.md`
